# Instabarato Frontend

Bootstrap inicial do frontend web da plataforma Instabarato.

## Estado atual

- stack base definida em Next.js + TypeScript
- `AGENTS.md` e `docs/` já preparados para novas sessões do Codex
- contrato local do backend copiado em `docs/contracts/backend-openapi.yaml`
- casca inicial do app criada em `src/app`

## Próximo passo

Na próxima sessão do Codex:

1. ler `AGENTS.md`
2. ler `docs/`
3. implementar foundation do app
4. montar sessão/auth e client base da API
