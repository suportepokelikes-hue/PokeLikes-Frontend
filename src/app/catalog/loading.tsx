import { PublicSurfaceLoading } from '@/components/ui/loading-surfaces';

export default function CatalogLoading() {
  return <PublicSurfaceLoading eyebrow="Catalogo publico" title="Carregando servicos..." description="Montando a vitrine publica." />;
}
